import SearchDashboard from "./SearchDashboard";

export const metadata = {
  title: "Search Properties | Panda Hotel",
  description: "Find your perfect stay",
};

export default function DashboardPage() {
  return <SearchDashboard />;
}
